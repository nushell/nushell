[package]
authors = ["The Nu Project Contributors"]
description = "Nushell Plugin"
edition = "2018"
license = "MIT"
name = "nu-plugin"
version = "0.29.1"

[lib]
doctest = false

[dependencies]
nu-errors = { path = "../nu-errors", version = "0.29.1" }
nu-protocol = { path = "../nu-protocol", version = "0.29.1" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
