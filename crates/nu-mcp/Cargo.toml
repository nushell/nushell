[package]
name = "nu-mcp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Modules to run a model context protocol (MCP) server that provides Nushell as a tool."
repository = "https://github.com/nushell/nushell/tree/main/crates/nu-mcp"
homepage = "https://www.nushell.sh"
license.workspace = true

[target.'cfg(unix)'.dependencies]
nix = { workspace = true, features = ["process"] }

[dependencies]
nu-engine = { workspace = true, features = ["os"] }
nu-json = { workspace = true, features = ["nu-protocol", "preserve_order"] }
nu-parser.workspace = true
nu-protocol = { workspace = true, features = ["os"] }
nuon.workspace = true

bytes = { workspace = true }
chrono = { workspace = true }
futures = "0.3.31"
hyper-util = { version = "0.1", features = ["tokio", "server-auto", "service"] }
indoc = "2.0.7"
miette = { workspace = true }
rmcp = { version = "3.1.0", features = ["server", "transport-io", "schemars", "transport-streamable-http-server"] }
schemars = "1.2.1"
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["full"] }
tokio-util = "0.7"
tracing = { version = "0.1.44", features = ["log"] }
tracing-subscriber = { version = "0.3.22", features = ["env-filter"] }

[features]
default = []
mcp = []

[lints]
workspace = true

[dev-dependencies]
nu-cmd-lang = { workspace = true, features = ["os"] }
