# config clear
clear the config

## Usage
```shell
> config clear {flags} 
 ```

## Flags
* -h, --help: Display this help message

## Examples
  Clear the config (be careful!)
```shell
> config clear
 ```

