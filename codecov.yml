coverage:
  status:
    project:
      default:
        target: 55%
        threshold: 2%

comment:
  layout: reach, diff, files
  behavior: default
  require_base: yes
  require_head: yes
  after_n_builds: 2
  
