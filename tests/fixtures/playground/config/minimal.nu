let-env config = {
	show_banner: false,
	color_config: {
		separator: { attr: n }
		leading_trailing_space_bg: { attr: n }
		header: { attr: n }
		empty: { attr: n }
		bool: { attr: n }
		int: { attr: n }
		filesize: { attr: n }
		duration: { attr: n }
		date: { attr: n }
		range: { attr: n }
		float: { attr: n }
		string: { attr: n }
		nothing: { attr: n }
		binary: { attr: n }
		cellpath: { attr: n }
		row_index: { attr: n }
		record: { attr: n }
		list: { attr: n }
		block: { attr: n }
		hints: { attr: n }
	},
	ls: {
		use_ls_colors: false
	}
}
