use std assert


# This is the custom command 1 for scope_commands:

#[test]
def scope_commands_show_the_commands_in_the_current_scope_1 [] {
  let result = (scope commands)
  assert ($result == )
}


