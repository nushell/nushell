use std assert


# This is the custom command 1 for keybindings_listen:

#[test]
def keybindings_listen_type_and_see_key_event_codes_1 [] {
  let result = (keybindings listen)
  assert ($result == )
}


